apache_conf_list = [subdomain_name,
					document_root, # Directory where code is located (Dummy File)
					static_file_dir, # Alias for /static
					static_file_dir, # Directory /static
					wsgi_dir, # Directory with wsgi.py inside
					manage_py_dir, # Python Path 
					virtual_env_site_packages, # Site Packages directory of virutal environment,
					wsgi_file, # Actual wsgi file path. 

]